# ── Core Pipeline ────────────────────────────────────────────────────────────
aiohttp==3.9.5          # async HTTP client (Stage 2 concurrent requests)
beautifulsoup4==4.12.3  # HTML parsing (LHN sidebar, listing pages)
lxml==5.2.1             # fast parser backend for BeautifulSoup4
pandas==2.2.2           # data processing, CSV output, STR calculations

# ── Dev / Testing ─────────────────────────────────────────────────────────────
black==24.4.2           # code formatter
pytest==8.2.0           # unit tests (STR formula, profit calc)
pytest-asyncio==0.23.6  # async test support
